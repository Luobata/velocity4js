/*
 * @description 定义好语言所需范式
 * @date 2016年10月24日15:58:08
 * @author luobata
 */

var bnf = function () {
    var statement = {
        reference: function (str) {
            // ::= "$" <identifier> { "." <method> | <identifier> }
            str.replace();
        }
    };

    return function (str) {
    }
}

module.exports = bnf;
